name: 🏁 Build Windows
on:
  workflow_call:
    inputs:
      working-directory:
        type: string
        required: false
  workflow_dispatch:

env:
  work-dir: ${{ github.workspace }}

jobs:
  build:
    strategy:
      matrix:
        arch: ["arm64", "x86_64"]
    name: Windows ${{ matrix.arch }}
    runs-on: windows-2022
    steps:
      - name: Set Working Directory
        shell: bash
        if: ${{ inputs.working-directory != '' }}
        run: echo "work-dir=${{ inputs.working-directory }}" >> $GITHUB_ENV

      - name: Setup Bazel
        uses: bazel-contrib/setup-bazel@0.19.0
        with:
          cache-save: ${{ github.event_name != 'pull_request' }}
          disk-cache: windows-${{ matrix.arch }}
          external-cache: true

      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Setup
        shell: bash
        working-directory: ${{ env.work-dir }}
        run: |
          python ./setup.py

      - name: Build
        shell: bash
        working-directory: ${{ env.work-dir }}
        run: |
          python ./build.py desktop --arch ${{ matrix.arch }} --output build/${{ matrix.arch }}

      - name: Upload Artifacts
        uses: actions/upload-artifact@v7
        with:
          name: GDMP-windows-${{ matrix.arch }}
          path: |
            ${{ env.work-dir }}/build/*
