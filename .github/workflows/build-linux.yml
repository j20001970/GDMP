name: 🐧 Build Linux
on:
  workflow_call:
    inputs:
      working-directory:
        type: string
        required: false
  workflow_dispatch:

env:
  work-dir: ${{ github.workspace }}

jobs:
  build:
    strategy:
      matrix:
        arch: ["arm64", "x86_64"]
        include:
          - arch: "arm64"
            os: "ubuntu-24.04-arm"
            runtime-arch: "aarch64"
          - arch: "x86_64"
            os: "ubuntu-24.04"
            runtime-arch: "x86_64"
    name: Linux ${{ matrix.arch }}
    runs-on: ${{ matrix.os }}
    env:
      SDK_VERSION: "25.08"
    steps:
      - name: Set Working Directory
        if: ${{ inputs.working-directory != '' }}
        run: echo "work-dir=${{ inputs.working-directory }}" >> $GITHUB_ENV

      - name: Setup Bazel
        uses: bazel-contrib/setup-bazel@0.19.0
        with:
          cache-save: ${{ github.event_name != 'pull_request' }}
          disk-cache: linux-${{ matrix.arch }}
          external-cache: true

      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Prerequisites
        run: |
          sudo apt update
          sudo apt install -y flatpak
          sudo flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
          sudo flatpak install --system -y flathub runtime/org.freedesktop.Sdk/${{ matrix.runtime-arch }}/${{ env.SDK_VERSION }}

      - name: Setup
        working-directory: ${{ env.work-dir }}
        run: |
          ./scripts/flatpak_env.sh << EOF
          python ./setup.py
          EOF

      - name: Build
        working-directory: ${{ env.work-dir }}
        run: |
          ./scripts/flatpak_env.sh --filesystem=home << EOF
          python ./build.py desktop --arch ${{ matrix.arch }} --output build/${{ matrix.arch }}
          EOF

      - name: Upload Artifacts
        uses: actions/upload-artifact@v7
        with:
          name: GDMP-linux-${{ matrix.arch }}
          path: |
            ${{ env.work-dir }}/build/*
