diff --git a/MODULE.bazel b/MODULE.bazel
--- a/MODULE.bazel
+++ b/MODULE.bazel
@@ -1,5 +1,24 @@
 module(name = "mediapipe")
 
+emsdk_version = "4.0.11"
+bazel_dep(name = "emsdk", version = emsdk_version)
+git_override(
+    module_name = "emsdk",
+    remote = "https://github.com/emscripten-core/emsdk.git",
+    strip_prefix = "bazel",
+    commit = "d49219d03a41cd12f95a33ba84273c20d41fd350",
+    patches = ["//patch:emsdk_link_wrapper.diff"],
+    patch_strip = 2,
+)
+emscripten_cache = use_extension(
+    "@emsdk//:emscripten_cache.bzl",
+    "emscripten_cache",
+)
+emscripten_cache.configuration(flags = ["--pic"])
+emscripten_cache.targets(targets = [
+    "crtbegin",
+])
+
 bazel_dep(name = "bazel_skylib", version = "1.8.2")
 bazel_dep(name = "platforms", version = "0.0.11")
 single_version_override(
