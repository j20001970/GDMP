# Based on https://github.com/google-ai-edge/mediapipe/pull/6284
diff --git a/WORKSPACE b/WORKSPACE
--- a/WORKSPACE
+++ b/WORKSPACE
@@ -228,5 +228,9 @@ http_archive(
 http_archive(
     name = "cpuinfo",
+    patch_args = ["-p1"],
+    patches = [
+        "@GDMP//patch:cpuinfo.diff",
+    ],
     sha256 = "9213f6f81784eb8679f0621ad1c20eac711e063cb9c7712738720609cbdf1c33",
     strip_prefix = "cpuinfo-ea6b9f1bb6e1001d8b21574d5bc78ddef62e499d",
     urls = [
@@ -264,6 +268,7 @@ http_archive(
         # (tensorflow/lite/build_def.bzl) where link_extra_lib is duplicated when rules_cc has
         # a version-suffixed canonical name (e.g., under single_version_override or complex dependency graphs).
         "@//third_party:org_tensorflow_combine_cc_tests_link_extra_lib.diff",
+        "@GDMP//patch:org_tensorflow_icu_windows_arm64.diff",
     ],
     sha256 = _TENSORFLOW_SHA256,
     strip_prefix = "tensorflow-%s" % _TENSORFLOW_GIT_COMMIT,
diff --git a/mediapipe/BUILD b/mediapipe/BUILD
--- a/mediapipe/BUILD
+++ b/mediapipe/BUILD
@@ -179,6 +179,14 @@ alias(
 # Windows 64-bit.
 config_setting_and_platform(
     name = "windows",
+    constraint_values = [
+        "@platforms//os:windows",
+    ],
+    visibility = ["//visibility:public"],
+)
+
+config_setting_and_platform(
+    name = "windows_x86_64",
     constraint_values = [
         "@platforms//os:windows",
         "@platforms//cpu:x86_64",
@@ -186,6 +194,15 @@ config_setting_and_platform(
     visibility = ["//visibility:public"],
 )
 
+config_setting_and_platform(
+    name = "windows_arm64",
+    constraint_values = [
+        "@platforms//os:windows",
+        "@platforms//cpu:arm64",
+    ],
+    visibility = ["//visibility:public"],
+)
+
 # Linux 64-bit.
 config_setting_and_platform(
     name = "linux",
diff --git a/platform_mappings b/platform_mappings
--- a/platform_mappings
+++ b/platform_mappings
@@ -55,7 +55,10 @@ flags:
     @mediapipe//mediapipe:ios_arm64e_platform
 
   --cpu=x64_windows
-    @mediapipe//mediapipe:windows_platform
+    @mediapipe//mediapipe:windows_x86_64_platform
+
+  --cpu=arm64_windows
+    @mediapipe//mediapipe:windows_arm64_platform
 
   --cpu=k8
     @mediapipe//mediapipe:linux_platform
